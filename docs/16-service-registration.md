# Lab 16 — Service registration

## What you will have at the end

AWX registered as a service behind the gateway — one login for the platform.

## Outline (to be written)

- [ ] Wait on `/api/gateway/v1/ping/`
- [ ] Register service node, cluster, routes via the gateway REST API (the installer's approach — not exec-ing manage commands)
- [ ] Local authenticator setup
- [ ] Verify: login at the gateway reaches the AWX API and UI; the Lab 14 job still runs

Back to the [README](../README.md) — you built an automation platform by hand.
